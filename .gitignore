node_modules/
dist/
outputs/*.json
!outputs/.gitkeep
!outputs/rtp-convergence.html
!data/.gitkeep
results/*.json
!results/.gitkeep
results/merged/*.json
!results/merged/.gitkeep
.DS_Store
.browser-profile/

# run products — this verifier writes them fresh each run; the committed artifacts are pinned
outputs/run/
